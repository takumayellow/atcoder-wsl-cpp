# agc017 A — Biscuits

- 問題: https://atcoder.jp/contests/agc017/tasks/agc017_a
- 提出（2026-08-04, Python）: [AC](https://atcoder.jp/contests/agc017/submissions/78114748)（1 回目で AC）
- 手書き原本: `Box/Photo Backup/手書き/atcoder/agc017/a/`（撮影 2026-09-25。`20260925_174844.jpg` = 定式化、`20260925_174809.jpg` = 場合分けと証明の 2 枚）
- 再現: [thinking.pdf](thinking.pdf)（原本の再現。赤は後から入れた振り返り）

## メモで考えたこと

1. 袋を並べて描き、最初に「$A_i \bmod 2 = P$」と書いて消した（1 袋ずつの偶奇では決まらない）。
2. 選ぶかどうかを $c_k \in \lbrace 0,1 \rbrace$ とし、$\bigl(\sum c_k A_k\bigr) \bmod 2 = P$ となる $c$ の数え上げ、と定式化した。
3. 素直に全部試すと $2^N$ 通りで、$N = 50$ だと $2^{50}$ なので無理（メモに「$2^{50}$ ×」）。
4. サンプル `1 1 / 50` → 0、`2 0 / 1 3` → 2、`3 0 / 1 1 1` → 4 を見て場合分けした。
   - 全部の $A_i$ が偶数: 和は必ず偶数。$P = 0$ なら $2^N$、$P = 1$ なら $0$。
   - 奇数の $A_k$ がある: $k$ 番目以外の $N-1$ 袋の選び方は $2^{N-1}$。その和 $S$ が奇数なら $k$ を選べば偶数・選ばなければ奇数、$S$ が偶数なら逆。
     どちらの $P$ に対しても **$k$ の選び方がちょうど 1 通りに決まる**ので、答えは $2^{N-1}$。

## つまずき

- メモでは 1 つ目の場合を「$\forall A_i = $ odd」と書いているが、結論（$P=0$ なら $2^N$、$P=1$ なら $0$）が成り立つのは**全部偶数**のとき。直後の行が「$A_i$ has odd（$\exists k,\ A_k = $ odd）」なので、書き間違い。
  コードは `a[i] % 2 == 1` の有無で分けていて正しい（サンプル 3 つと、$N \leq 6$ の全探索との突き合わせ 200 ケースで一致を確認）。
- 最初の「$A_i \bmod 2 = P$」は、和の偶奇を 1 項ずつの偶奇と混同した一歩目。すぐ消して $\sum$ の形に直せている。

## 正しい考え方

奇数の袋が 1 つでもあれば、それを「偶奇を調整する袋」として取っておくと、残りの選び方と 1 対 1 に対応する。
よって答えは、奇数の袋があれば $2^{N-1}$、無ければ $P = 0$ のとき $2^N$、$P = 1$ のとき $0$。

図は thinking.pdf の最後のページ。

## 次に活かすこと

- 「和の偶奇が指定された部分集合の数」は、**調整役を 1 つ決めて残りを自由にする**全単射で数える。
- 場合分けを書いたら、「$\forall$ / $\exists$」「odd / even」が次の行の結論と合っているか読み直す。
