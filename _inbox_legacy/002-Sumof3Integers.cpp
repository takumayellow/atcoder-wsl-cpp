#include <iostream>
using namespace std;

int main()
{
    int a1, a2, a3;        // ① 変数を宣言
    cin >> a1 >> a2 >> a3; // ② 3 つまとめて読み込む
    cout << a1 + a2 + a3;  // ③ 合計を表示
    return 0;              // ④ 正常終了を示す
}
