# Progress

開始位置は `A04` です。現状の実装済みは `A01` から `A04` までで、次に解く問題は `A05` です。

## 進め方

問題ディレクトリのコードを編集:

```sh
a/a05/main.cpp
```

単体実行:

```sh
scripts/run_cpp.sh a/a05 a/a05/test/sample-1.in
```

サンプル一括検証:

```sh
scripts/test_samples.sh a/a05
```

## メモ

- `A04` は実装済みでサンプル通過済み
- 未着手は `A05` 以降
